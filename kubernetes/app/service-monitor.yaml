---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cloud-app-service
  namespace: default
spec:
  endpoints:
    - honorLabels: true
      port: cloud
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: cloud-app-service