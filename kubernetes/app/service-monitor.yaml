---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cloud-app-service
spec:
  endpoints:
    - honorLabels: true
      port: cloud
  selector:
    matchLabels:
      app: cloud-app-service