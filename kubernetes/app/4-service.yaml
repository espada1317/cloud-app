apiVersion: v1
kind: Service
metadata:
  name: cloud-app
  namespace: monitoring
  labels:
    app: cloud-app
spec:
  type: ClusterIP
  ports:
    - name: cloud
      port: 8080
      protocol: TCP
  selector:
    app: cloud-app