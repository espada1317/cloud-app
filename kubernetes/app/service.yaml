apiVersion: v1
kind: Service
metadata:
  name: cloud-app-service
  namespace: default
  annotations:
    prometheus.io/port: "metrics"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/part-of: cloud-app-monitor
spec:
  selector:
    app: cloud-app-pod
  ports:
    - name: "metrics"
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer